# Routes deliberately excluded from byte-for-byte parity. Each needs a reason and a
# Phase-6 follow-up. The ONLY legitimate exclusions are routes that cannot be captured
# deterministically AND are out of scope per AUDIT.md §10 (e.g. timing-dependent SSE).
#
# Prefer a per-route `mask` in routes.yaml (neutralize the few random/wall-clock bytes and
# still byte-compare everything else) over a whole-route exclusion. These 3 remain pending a
# mask/stream-capture follow-up; do NOT add new exclusions to dodge hard routes.

exclusions:
  - id: get__tail
    reason: "SSE live-tail stream; needs bounded-stream capture + Go pub/sub. In progress."
    followup: "Implement Go SSE + pub/sub; capture the deterministic opening frame."
    owner: "TBD"
  - id: get__rum
    reason: "Full RUM page; error-rate sparkline has chdb now()-derived bucket timestamps. Port handler + mask the sparkline t-values. In progress."
    followup: "Port view_rum; mask the 180 sparkline timestamps."
    owner: "TBD"
  - id: get__settings_data-management
    reason: "DB-stats page; system.parts byte sizes vary. Port page + mask the size cells. In progress."
    followup: "Port the page; mask the formatted byte-size cells."
    owner: "TBD"
