# Routes deliberately excluded from byte-for-byte parity. Prefer a per-route `mask` in
# routes.yaml (neutralize the few random/wall-clock bytes, byte-compare everything else) or a
# bounded `stream` capture over a whole-route exclusion. None are excluded — every authoritative
# route is GREEN (some via documented field masks for inherently-random/wall-clock output).

exclusions: []
