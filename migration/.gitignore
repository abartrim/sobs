# Runtime / generated artifacts — not source.
fixtures/data/
fixtures/_run/
fixtures/_gate0.chdb/
golden/RESULTS.json
tools/__pycache__/

# The golden corpus is regenerated from the frozen Python app (bootstrap_phase1.py /
# capture_golden.py). Not committed: large binary blobs, and some captures (static-file
# headers) carry environment-derived mtimes. Regenerate locally to run parity_check.
golden/
_run/
