# Parity Ledger

_This file is auto-generated by `parity_check.py`. The placeholder below is replaced on
the first run. It is the migration burndown — the single number that matters is RED._

**GREEN 0 / RED 0 / MISSING 188 / EXCLUDED 0 / total 188**

Run `python migration/tools/parity_check.py --update-ledger` to populate.

Status legend:
- 🟢 GREEN — Go output is byte-identical to the golden (after normalize allow-list).
- 🔴 RED — Go output differs. The route is not done. Work it (see GOAL.md priority order).
- ⚪ MISSING — no golden captured yet (run `capture_golden.py`) or no Go handler.
- ⚫ EXCLUDED — documented in `manifest/EXCLUSIONS.yaml` with a Phase-6 follow-up.

The migration is complete when **RED == 0 and MISSING == 0** and EXCLUDED is only the
documented set.
