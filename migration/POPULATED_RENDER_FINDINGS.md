# Populated-render divergences found by corpus expansion

These Go bugs were latent because the original golden corpus captured every page with **empty**
data — so the populated rendering branches (counts, filters, querystrings, status badges) were
never byte-compared. Seeding `view_logs` (`logsview` profile) and the `view_traces` list path
(reusing the `tracedetail` seed) exposed five genuine divergences. Each fix only makes Go match the
Python/Quart/Werkzeug/Jinja2 oracle — it can never regress empty-corpus parity.

| # | Symptom (golden vs Go) | Root cause | Fix |
|---|---|---|---|
| R1 | `level_stats`/`service_stats` rendered `ERROR: 3.0` not `ERROR: 3` | `COUNT()` arrives as float64; Python keeps it an int | `cInt(m,"cnt")` in `computeLogStats` (`handlers_pages_logs_errors.go`) |
| R2 | `url_for` querystring over-encoded: `sql=has_tag%28%27env%27%2C%27prod%27%29` vs `has_tag('env','prod')` | Go `url.QueryEscape` only leaves `A-Za-z0-9-_.~` literal; Werkzeug 3.1.8 `_urlencode` (quote_plus) also leaves `! $ ' ( ) * , / : ; ? @` literal | `werkzeugQueryEscape` relaxes those 12 codes (`render.go`) |
| R3 | filter pages dropped the raw `from_ts`/`to_ts` input value (`value=""` on a time error) | `request.args` was hardcoded to an empty map in `baseContext` | `renderPageReq` populates `request.args` from `r.URL.Query()` (`handlers_pages.go`); `view_logs` switched to it |
| R4 | HTTP status badge wrong: `404` got `bg-success` not `bg-warning` (`span.http_status\|string >= '400'`) | `compareOrd` only compared numbers; any non-numeric operand returned `false` | lexicographic string fallback in `compareOrd` (`eval.go`) |
| R5 | `view_traces` list path 500'd: `unsupported filter "string"` | Jinja `\|string` (soft_str) filter not implemented | added `case "string": return pyStr(val)` (`eval.go`) |
| R6 | `url_for(... grouped=_g ...)` rendered `grouped=%3Cnil%3E` vs Python omitting it (where `{% set _g = '1' if grouped_mode else none %}`) | Go `urlFor` rendered `fmt.Sprintf("%v", nil)` = `<nil>`; Werkzeug omits None-valued query params (empty string is kept as `k=`) | skip `kw[k] == nil` in `urlFor` (`render.go`) — found by the `errorsview` batch |
| R7 | incident window `from_ts`/`to_ts` rendered `2023-06-01 11:45:00 +0000 UTC` vs Python `2023-06-01 11:45:00.000000` | `normalizeCHTimestamp(v any)` was called with a `time.Time` (`dt.Add(-half)`); `toStr(time.Time)` yields Go's `String()` which no layout parses → fell through to the raw value. Python `_normalize_ch_timestamp` handles `datetime` directly | add a `time.Time` branch → `t.UTC().Format("2006-01-02 15:04:05.000000")` (`mutation_helpers.go`) — found by the `view_incident` batch |
| R8 | `view_ai` rows rendered empty model/tokens/provider (`data-model=""`, tokens 0) for spans whose attrs were present in the DB | `attrMap(cStr(r, "SpanAttributes"))` — `cStr` `fmt.Sprintf`s the chdb map into an unparseable `"map[...]"` string → `mapToDict` returns `{}`. Working paths pass the RAW value | `attrMap(r["SpanAttributes"])` (`handlers_pages.go`) — found by the `aiview` batch |
| R9 | `view_ai` `ai_pricing_json` omitted observed models (e.g. `claude-opus` inferred entry) | view_ai rendered a STATIC embedded `savedAiPricingJSON` blob; Python `_load_ai_pricing_with_sources` dynamically merges defaults + observed-model inference + saved/confirmed | call `s.loadAiPricingWithSources()` (already implemented for save_ai_settings) in view_ai (`handlers_pages.go`) — found by the `aiview` batch |
| R10 | `view_metrics` + `view_metrics_anomaly` rendered `Sample` `5.0` / `point_count` as floats vs Python ints | `last_sample_count`/`point_count`/`sample_count` are UInts (`argMax(SampleCount,…)`/`count()`) passed raw (float64 from chdb); Python renders the int. `value`/`baseline_*`/`anomaly_score` are genuine Float64 → kept raw | `cInt(m, …)` for the count columns in both handlers (`handlers_pages.go`) — found by the `metricsauto` batches (same class as R1) |
| R11 | `save_ai_settings` returned 302 (success) on invalid `model_pricing`/`model_pricing_confirmed` JSON; Python returns 400 `{"error": ...}` | the Go POST handler was a simplified port — it wrote the basic ai.* keys but omitted the pricing/confirmed JSON validation+normalize and the github_token expiry/validation-reset logic | ported the validation (parse → 400 on bad JSON, normalize entries, save) + github_token branch (`handlers_pages.go`) — found by the `aisettings` batch |
| R13 | RUM event items rendered empty model/url/session attributes (`data-*=""`, anon session key) for `hyperdx_sessions` rows whose `LogAttributes` were present — affecting every `_build_rum_event_item` caller (`view_rum` event list, `view_incident` rum sections) | `buildRumEventItem` did `mapToDict(cStr(m, "LogAttributes"))` — `cStr` `fmt.Sprintf`s the chdb `Map(String,String)` (materialized as `map[string]any`) into an unparseable `"map[...]"` string → `mapToDict` returns `{}`, silently dropping all attrs (same class as R8). Latent: no prior golden carried RUM `LogAttributes` through this path | `mapToDict(m["LogAttributes"])` (raw value) + normalize both its `map[string]any` and `*jsonenc.Object` forms when building `attrsMap` (`handlers_rum.go`) — found by the `incidentmatch` batch (the `?rum_session=` MATCH path) |
| R14 | `get_ai_span_attributes` + `api_raw_span` rendered `raw_attrs` (a `json.dumps(_map_to_dict(SpanAttributes), indent=2)` string) with keys in ALPHABETICAL order vs Python's chdb-Map INSERTION order (same byte length, wrong order) | Go read the bare `Map(String,String)` column → `store.Execute` `json.Unmarshal`s it into a Go `map[string]any` (order lost) → `jsonDumpsIndent2`/`encoding/json` sorts keys; `api_raw_span` additionally used `mapToDictObject` which `sort.Strings`-ed | query `mapKeys(SpanAttributes)`/`mapValues(...)` (Array(String), order survives) + new `orderedMapFromKV` → order-preserving `*jsonenc.Object` (verified `mapKeys` order == oracle dict order, and `encoding/json` SetIndent on an ordered Object preserves order) (`handlers_misc.go`, `handlers_pathparam.go`) — found by the `aiview` span-attrs MATCH batch |
| R15 | `rum_asset_download` FOUND/download branch (`GET /v1/rum/assets/<id>`) emitted the wrong response-header set: Go sent `Accept-Ranges: bytes`, omitted `Cache-Control`, and sent `Content-Type: text/plain` (no charset) vs the oracle's `cache-control: public, max-age=43200`, `content-type: text/plain; charset=utf-8`, and NO `Accept-Ranges`. (Body + status were already correct.) | The Go port served the stored file via `http.ServeContent`, whose header model (Accept-Ranges, conditional/Range, no Cache-Control, no mimetype-charset) does NOT match Quart's `send_from_directory`/`send_file`. Quart appends `; charset=utf-8` via `werkzeug.utils.get_content_type` for text mimetypes, sets `Cache-Control: public, max-age=<send_file_max_age_default>` (12h = 43200), and does not advertise byte ranges in this config. Latent: no prior golden exercised the download branch (the empty fixture seeds no assets, so every valid id 404'd). | replaced the `http.ServeContent` block with a direct Quart-style header emission (`Content-Type` via new `werkzeugContentType` mirroring `get_content_type` + `_charset_mimetypes`, `Content-Length`, `Cache-Control: public, max-age=43200` = `staticMaxAge`) — the same approach `serveRumFile` already uses for the static `rum.*` downloads. The non-reproducible `Last-Modified`/`Expires`/mtime-format `ETag` are intentionally not emitted (normalize.py drops them from the golden) (`handlers_get2.go`) — found by the `rumasset` batch |
| R16 | `edit_chart` (`POST /dashboards/<id>/charts/<cid>/edit`) and `clone_chart` (`POST /dashboards/<id>/charts/<cid>/clone`) were UNIMPLEMENTED in Go for any case where the dashboard EXISTS: the router fell through to `http.NotFound` (404) for the chart-found success, chart-not-found, and form-validation branches. Only the dashboard-not-found 404 path worked (it flashes before the switch). | `handleDashboardsFormSub` (`handlers_forms.go`) routed the `/dashboards/<id>/...` subtree but its `switch` had cases only for `/delete`, `/charts`, and `/charts/<cid>/delete` — no `.../edit` or `.../clone` case, so both reached `default: http.NotFound`. Latent: the only committed edit/clone routes targeted a non-existent dashboard (`/dashboards/nope/...`), which short-circuits at the dashboard lookup, never reaching the missing branches. | added `editChart`/`cloneChart` + a shared `parseChartFormSubmission` (mirrors app.py `_parse_chart_form_submission`) and wired two new `switch` cases (`handlers_forms.go`). Both mirror app.py exactly: lookup the chart in `_get_charts` (404-flash "Chart not found" when absent), parse the form (warning-flash on ValueError), then re-INSERT (edit: existing id+position) / INSERT a new chart (clone: fresh uuid, next position). Both redirect to `view_custom_dashboard(dashboard_id=<existing id>)`, so the stored version-millis (edit) and the server-generated chart uuid (clone) NEVER reach the response — every branch's body is byte-stable (clone is **not** R12-deferred for this reason). Found + covered by the new `chartedit` profile (seeded dashboard `d0…d001` + chart `c0…c001`); 5 routes GREEN (edit success/chart-missing/validation-error, clone success/chart-missing). |
| R17 | `view_logs` raw-SQL query-EXECUTION error branch (`GET /logs?sql=<bad>`, app.py 11402-11404) rendered a different `error_msg` than the oracle: the chdb `UNKNOWN_IDENTIFIER` message echoes back the failing query verbatim, and Go's COUNT query carried an `AS c` alias the oracle's does not — so Go leaked `SELECT COUNT(*) AS c FROM otel_logs WHERE …` vs the oracle's `SELECT COUNT(*) FROM otel_logs WHERE …` (5-byte body diff at the error string). | `handleViewLogs` (`handlers_pages.go`) issued `SELECT COUNT(*) AS c FROM otel_logs …` and read the result via `cInt(rowMaps(res)[0], "c")`. app.py 11372 issues `SELECT COUNT(*) FROM otel_logs {query_where}` with NO alias. On a malformed user `sql` WHERE, chdb's exception text contains the exact SELECT it was given, so the alias divergence surfaces in `_public_dashboard_query_error`'s sanitized first line. Latent: no prior golden drove a query-execution failure through view_logs (the committed error routes hit the *validation* ValueError at 11321-11322 or the regex/time pre-try errors, never the inner try's except). | dropped the `AS c` alias to match app.py exactly and read the lone column positionally via `res.Columns[0]` (`handlers_pages.go`). The success path (`query_where != ""` filters: level/service/event/trace/sql) is unaffected — verified all 12 `logsview` goldens still GREEN. Found + covered by the new `logsrich` profile + `get__logs__logsrich_sql_exec_error` route (`sql=nonexistent_col=1` → chdb UNKNOWN_IDENTIFIER → SQL-error branch); GREEN. |
| R19 | `summary` (`GET /`) Signal Health panel rendered EVERY service's badge as `bg-success`/`bi-check-circle-fill` (`worst_state='normal'`) when the oracle renders the rule-driven state (e.g. `bg-danger`/`bi-exclamation-triangle-fill` `outlier`). No threshold/seasonal anomaly rule EVER fired in Go's `_get_signal_health_by_service` → `_annotate_rows_with_rules` chain, so `effective_state` always collapsed to `normal`. (Same class as R10: a chdb integer aggregate decoded as float64.) | `evaluateThresholdCondition` (`chart_anomaly_engine.go`) gates on `iStr(sampleCount)`, which mirrors Python `int(str(sample_count))`. chdb-go decodes `argMax(SampleCount, time)` (a UInt64) as a Go **float64** (10.0), whereas chdb-python hands back a Python **int** (10). So Python evaluates `int(str(10)) == 10` and the rule fires, but Go evaluated `int(str(10.0))` → `Atoi("10.0")` → error → `iStr` returns `ok=false` → `evaluateThresholdCondition` early-returns `nil` for EVERY rule → no match → `normal`. Latent: no prior golden carried logs/traces-source derived-signal rows through signal_health WITH a matching anomaly rule (the empty fixture has no recent derived signals; the seeded `metricsauto` family has no logs-source rules). | taught `iStr` to treat an INTEGRAL `float64` as the int Python received — `iStr(10.0) → (10, true)` while a fractional float still fails (mirroring `int(str(10.5))` raising) (`chart_anomaly_engine.go`). This makes Go's rule evaluation match the oracle on the genuine integer SampleCount/min_sample_count the driver delivers. Found + covered by the new `summaryrich` profile (now()-anchored otel_logs collapsed into one minute bucket → constant log_volume=10/error_volume=5/error_ratio=0.5 derived series + 11 logs-source anomaly rules exercising gt/lt × warn/crit, the min-sample-skip, the normal no-fire, the seasonal fallback, and both `_rule_matches_series` mismatch branches) + the `get__root__summaryrich` route; GREEN (verified twice across a forced now()-minute drift). Full Docker parity re-run after the fix: GREEN 613 / RED 0. |
| R20 | **COVERAGE-ONLY, NO Go fix.** The GitHub dependency-parse chain reached synchronously via `POST /api/enrichment/cve/scan` — `_fetch_release_deps_from_github` (app.py 16703-16873, orchestration), `_github_actions_dependency_rows` (16479-16629, GH-Actions snapshot path), and `_parse_package_lock_dependencies` (16358-16399) — had its SUCCESS path UNVERIFIED by any prior golden: the only seeded cve route (`cvebackfill`) gives the release an **empty `CommitSha`** and no contents fixtures, so the actions path returns immediately and every contents GET 404s (attempted=1, inserted=0, libraries 0). The whole parse/insert path was dark. | The existing `cvebackfill`/`seed_repo_app` seed never set a commit nor served any lockfile, so the actions runs/artifacts traversal and the contents-API fetch+parse+insert branches never executed under capture. (The Go port — `cve_scan.go` + `fix_cve_helpers.go` — was already complete and correct; this finding is coverage, not a fix.) | NEW `depsrich` profile (`seed_depsrich`: `seed_repo_app` rows but `CommitSha="deadbeefcafe"`) + 3 canned upstream fixtures: `actions/runs?…head_sha=deadbeefcafe` → one `conclusion:"success"` run (id 555); `actions/runs/555/artifacts` → an artifact whose `name`≠`sobs-release-dependency-snapshots` (so `snapshot=None`/`nil` → no zip download → empty actions rows in BOTH runtimes); `contents/requirements.txt` 404 (no fixture) → loop advances; `contents/package-lock.json` → base64 package-lock exercising the modern `packages` branch (root `""`/`"."` skip, non-`node_modules/` skip, valid dep, missing-version skip, **nested** `node_modules/a/node_modules/<x>` last-segment, and a name+version **dup** → `seen` dedup) → 3 npm deps → ONE `dependencies-lockfile` artifact INSERTED → `_collect_library_inventory` tier-1 → OSV (shared canned vuln) → 3 findings. Route `post__api_enrichment_cve_scan__depsrich` (mask `scanned_at`); golden = `{attempted:1, inserted:1, max_releases:300, libraries_found:3, vulns_found:3}`. GREEN; existing 3 cve_scan routes re-verified GREEN (no regression). |
| R21 | `GET /api/work-items` did NOT reflect the GitHub work-item backfill: under a seeded `ai.github_token` + stale work items, the oracle's response carries the REFRESHED rows (issue state/title, linked PR, derived Copilot status), but Go returned the OLD seeded values unchanged (85-byte body diff; both 200). | app.py `api_get_work_items` (18527-18528) **AWAITS** `_maybe_backfill_github_work_item_links(db, settings)` SYNCHRONOUSLY before the SELECT, so the rows it re-inserts ARE visible in the same response. The Go `handleApiWorkItems` (`handlers_data.go`) explicitly SKIPPED the backfill — its comment assumed it was "fire-and-forget … owned elsewhere" (true only for the *other*, background `view_work_items` page caller at app.py ~18420, which Go correctly launches as `go s.maybeBackfillGithubWorkItemLinks()` in `handlers_pages.go:969`). Latent: the only prior `/api/work-items` goldens used the EMPTY corpus (no github token → the backfill's missing-token early-return → no DB writes → identical empty `{ok:true,items:[]}`), so the divergence never surfaced. | call `s.maybeBackfillGithubWorkItemLinks()` SYNCHRONOUSLY (not a goroutine) at the top of `handleApiWorkItems` (`handlers_data.go`), mirroring app.py's `await`. The function is self-contained (reads `ai.github_token` itself, so the Python `_load_all_ai_settings` is implicit) and token-gated, so the empty-corpus golden is unaffected. **The rest of the chain (`backfillGithubWorkItemLinks`, `searchOpenPRForIssue`, `deriveCopilotAssignmentStatus`, `serializeGithubWorkItemRow`, and crucially the GitHub search-URL `q=` query encoding) was ALREADY byte-faithful** — verified the httpx-`params` encoded URL and Go's `url.QueryEscape(q)+"&per_page=1"` produce IDENTICAL fixture keys for the spaces/`:`/`/`/`"`/`#` in `q` (both `b153a5a0…` for issue #7's search). Found + covered by the new `workitems` profile (`seed_workitems`: token + 3 stale work items) + 6 canned upstream fixtures (issue-GET + PR-search per item), exercising the closed→`completed`, copilot-assignee→`active`, and linked-PR→`blocked` arms of `_derive_copilot_assignment_status`. Route `get__api_work_items__workitems` (no mask — serializer emits no wall-clock value; fixed 2024 timestamps); golden = 3 refreshed items, CreatedAt DESC. GREEN; full Docker re-run after the fix: GREEN 643 / RED 0. |
| R22 | **COVERAGE-ONLY, NO Go fix.** `_fetch_trace_metric_context` (app.py 14959-15306) — the trace-detail "metrics retention context" ranked-match fetch reached synchronously via `GET /traces?trace_id=` (view_traces, app.py 15580) — had its **entire SUCCESS path UNVERIFIED** by any prior golden. Every committed trace profile (`tracedetail`/`tracesrich`) seeds the trace at a fixed 2023 epoch with NO matching metric rows, so the function always took its `match_mode='none'` no-data fallback (app.py 15299-15306) and the populated branch (15258-15297: source/match/dimension badges, the metric series, `_query_timeseries` buckets, `_group_metric_series` groups, `_compute_health_chips` + `header_chip`) was dark. | The otel_metrics 48h baseline TTL is evaluated against the real wall clock (app.py 2365, `MODIFY TTL TimeUnixMs + INTERVAL 48 HOUR`), so a fixed-epoch metric row is silently TTL-dropped on the seed's `OPTIMIZE … FINAL` and never matches a fixed-epoch trace — the path was structurally unreachable from the existing fixed-2023 trace seeds. (The Go port — `fetchTraceMetricContext` in `handlers_pages.go` + the Jinja render of `trace_detail.metrics_context` in `templates/traces.html` — was already complete and byte-faithful; this finding is coverage, not a fix.) | NEW `tracemetrics` profile (`seed_tracemetrics`): a **now()-anchored single-span trace** carrying k8s identity attrs (namespace=shop / pod=cart-7d / node=node-a / deployment=cart, service=cart-api) PLUS three **now()-anchored** `otel_metrics_gauge` rows sharing those attrs + ServiceName (`k8s.pod.cpu.utilization`=45.0, `k8s.pod.memory.usage`=536870912.0, `k8s.pod.status_phase`=1.0). Trace + metrics share ONE `toStartOfMinute(now())-30min` instant, so the FIRST ranked attempt "pod_exact" (pod + namespace, app.py 15180-15189) returns data and the loop short-circuits on tier 1: golden = `source_mode=raw` (gauge→SourceRank 0), match badge "pod + namespace" + dims [namespace, pod], 3 series (each `points=1`, ordered by MetricName ASC), 24-bucket `_query_timeseries` (values land in bucket 11), 2 `_group_metric_series` groups (Resource Pressure cpu+memory, K8s State status_phase), 2 health chips (CPU 45.0% level-ok = header_chip, Memory 0.5GB) — every value/badge byte-compared. Route `get__traces__tracemetrics` (4 masks: the DateTime64 trace_start/end_ts pair + the match-case `_traceMetricChartData` traceStart/traceEnd epoch-ms + the now()-derived ticks_ms array; all metric values/badges/bucket-counts stay byte-compared). GREEN 1 / RED 0 — the Go side computes the identical bucket index, series values, chips, groups, and match badges, confirming the port is correct on this path. **Residual uncovered (classified, not chased):** tiers 2-6 (node+ns / deployment+ns / service_exact / service_family / time_window_only, app.py 15191-15251) are mutually exclusive with the firing tier-1 under one trace → SCHEDULABLE later, not a divergence; `source_mode` pinned/mixed (15127-15130, needs `*_pinned` rows) → SCHEDULABLE; the `_query_timeseries` default-parse and `_query` default-parse FALLBACK loops (15050/15092, run only when the utc parse yields nothing) → DEFENSIVE; the `_query_timeseries` try/except (15286-15287) and the final no-match return (15299-15306) → DEFENSIVE. |
| R23 | `view_errors` (`GET /errors`) error-header summary diverged for the `_extract_structured_error_summary` **`json.dumps(parsed, ensure_ascii=False)` compact fallback** (app.py ~10647) AND for the `_try_pretty_json_text` indent-2 body block (app.py ~10565) whenever the payload had **non-ASCII** or **>1 key**: (a) the compact fallback emitted `{"a":{},"b":[]}` (no spaces) in ALPHABETICAL key order vs the oracle's `{"a": {}, "b": []}` (default `", "`/`": "` separators) in INSERTION order; (b) the pretty body escaped non-ASCII (`naïve`) vs the oracle's literal `naïve`. | TWO Go serializer divergences, both surfaced by the new `errorsummary` rows. (a) `extractStructuredErrorSummary` (`handlers_incident.go`) parsed via `parseJSONNative` (a `map[string]any`, order lost) and dumped via `jsonDumpsNoEscErr` (Go `json.Encoder` → compact no-space separators + SORTED map keys). Python's bare `json.dumps(x, ensure_ascii=False)` uses DEFAULT separators `", "`/`": "` and preserves dict INSERTION order. (b) `jsonDumpsIndent2NoEsc` (`handlers_misc.go`, used by `tryPrettyJSONText` for `message_pretty_json`/`raw_body_pretty_json`/`stack_pretty_json`) used Go's `encoding/json` which ALWAYS escapes non-ASCII to `\uXXXX`; Python's `json.dumps(..., ensure_ascii=False, indent=2)` keeps literal UTF-8. Latent: the only prior `/errors` golden (`errorsview`) used plain-text messages (always the `_compact_text(message)` False branch), so neither JSON path was ever byte-compared. | (a) `extractStructuredErrorSummary` now parses with the order-preserving `parseJSONValue` (→ `*jsonenc.Object`) and dumps via `jsonenc.Encode(parsed, Options{SortKeys:false, EnsureASCII:false, ItemSep:", ", KeySep:": "})`; `firstScalarFromJSON`/`summaryFromParsed` gained a `*jsonenc.Object` case so the summary descend pass is ALSO insertion-order-faithful (a latent nondeterminism: Go map iteration could pick a different "first non-empty" scalar than Python). Removed the now-dead `parseJSONNative`/`jsonDumpsNoEscErr`. (b) added an `Indent` field to `jsonenc.Options` (empty = unchanged single-line behavior, so NO existing golden shifts) implementing Python `indent=2` exactly (newline+2-space per item, empty `{}`/`[]` inline, `,` item sep, `": "` key sep); `jsonDumpsIndent2NoEsc` now delegates to `jsonenc.Encode(..., Indent:"  ")` so non-ASCII stays literal. Verified the new indent path is byte-identical to the old `encoding/json` output for ASCII inputs (so the pretty-JSON body of every other route is unchanged) and keeps unicode literal. Found + covered by the new `errorsummary` profile (14 otel_logs error rows whose exception.message/Body span every branch: message+type+code extras, nested/list descent, type/code/scalar-value descend, substring-skip extras, **the compact-dumps fallback incl. unicode + insertion order — r13 `{"a": {}, "b": []}` / r14 `{"über": {}, "x": []}`**, invalid-JSON continue, non-{/[ continue, raw_body-JSON success, plain fallback) + the `get__errors__errorsummary` route (no mask — `/errors` applies no time-window filter without from_ts/to_ts, so the fixed timestamps need no now()-anchoring). GREEN 1 / RED 0. |

| R24 | `view_ai` (`GET /ai?view=trace`) AI-trace turn-card / GenAI message render: a GenAI message carrying a top-level `tool_calls` (or a `parts` `tool_call`, or a `function_call`) whose `arguments` is a JSON object with an **integer** value rendered the integer as a **float** — golden `tool_call:calc {"n": 2}` vs Go `tool_call:calc {"n": 2.0}` (4-byte body diff; surfaced at byte 155189 of the trace-view page, in both the span-summary `title=` and the body). | `parseGenaiMessagesJSON` (`go/cmd/sobs/ai_view.go`) decoded the `gen_ai.input.messages` / `gen_ai.output.messages` JSON with a plain `json.Unmarshal([]byte, &any)`, which promotes EVERY JSON number to `float64`. When `_genai_tool_calls_to_text` (app.py 8234) re-serializes the `arguments` dict via `json.dumps(arguments, ensure_ascii=False)`, the Go side then emitted `2.0` where Python's `json.loads` had kept `2` an `int` and `json.dumps` re-emitted `2`. (The same promotion would corrupt the `json.dumps(part)` fallthrough at app.py 8283 and the reasoning-dict dumps at 8330 for any int-valued payload.) Latent: every prior `/ai` golden (`aiview`/`airich`) seeded EMPTY or non-JSON message attrs, so the GenAI message parsers returned empty and no `json.dumps` of a parsed number was ever byte-compared. | switched `parseGenaiMessagesJSON` to a `json.Decoder` with `UseNumber()` so integer literals survive as `json.Number` (objects stay `map[string]any`, so the message parsers' existing `map[string]any` type assertions are untouched; `jsonenc.Encode` already renders `json.Number` verbatim and `toStr(json.Number)`/`isEmptyArg` handle it correctly). This makes the whole cluster (`_genai_message_content_to_text` content-str/content-list/parts[text,reasoning,tool_call,tool_call_response]/tool_calls/function_call arms, `_genai_message_reasoning_to_text`, `_extract_messages_text`, `_genai_tool_calls_to_text`) int-faithful. Found + covered by the new `aiturns` profile (`seed_aiturns`: ONE trace `aiturnstrace1`, 5 AI spans across 2 `gen_ai.turn_ids`, each carrying a distinct message-shape JSON) + the `get__ai__aiturns` route (`view=trace`; NO mask — fixed 2023 timestamps + UInt64 Duration, and `/ai` applies no time-window without from_ts/to_ts, so every ts/duration is constant). Golden confirms: 2 ordered turn cards (`_build_ai_trace_turn_cards` group/sort/enumerate), user/assistant from content-str + content-list-parts (`hello there` / `hi friend`), accumulated+rounded `duration_ms` (2200.0 / 1300.0), status Completed/In Progress, and the int-preserved `tool_call:calc {"n": 2}`. Scoped GREEN 1 / RED 0; full Docker barrier after the fix: **GREEN 646 / RED 0 / MISSING_GOLDEN 0** (no regression — every other route's empty/non-JSON message path is unaffected). |
| R25 | **COVERAGE-ONLY, NO Go fix.** Notification channel DISPATCH cluster (`POST /api/notifications/channels/<id>/test` → `test_notification_channel` app.py ~25882 → `_dispatch_notification_channel` ~25058 → the per-type fns). The **webhook** branch was already covered (`post__api_notifications_channels__test__seeded`, profile `notif`), but the **slack** (`_dispatch_slack_channel` ~24842), **browser_push** (`_dispatch_browser_push_channel` ~24895 + `_encrypt_push_payload` ~24990 + `_build_vapid_jwt` + VAPID/JWT path), **unknown-channel-type**, and **hub-exception** branches had their SUCCESS/error paths UNVERIFIED by any prior golden — no seeded channel of those types existed. | The route response is the constant `{"ok": true}` on success (or `{"ok": false, "error": result}, 500` on failure): the wall-clock `fired_at` and the random Web-Push ciphertext go ONLY into the OUTBOUND POST body, which the mock-upstream harness IGNORES (it keys fixtures on `sha256("METHOD url")[:32]`, never the request body). So the byte-compared response is timestamp/ciphertext-independent — **no now()-anchoring, no masks** — yet Python coverage still credits every dispatch line because Python executes them. The Go port (`dispatchNotificationChannel`/`dispatchWebhookChannel`/`dispatchSlackChannel`/`dispatchBrowserPushChannel`/`encryptPushPayload`/`buildVapidJWT`/`loadVapidPrivateKey` in `upstream.go` + `notify_dispatch.go`, handler `handleChannelSub` in `handlers_mutations3.go`) was ALREADY complete and byte-faithful — both runtimes return identical responses on all five arms — so this is coverage, not a fix. | NEW `notifydispatch` profile (`seed_notifydispatch`: 5 channels — webhook/slack/push success + unknown-type + no-fixture-webhook-fail) + profile env `SOBS_UPSTREAM_FIXTURES` + a FIXED structurally-valid VAPID P-256 PKCS8-DER private key in `SOBS_VAPID_PRIVATE_KEY` (read identically by app.py `_get_vapid_private_key_b64` and Go `loadVapidPrivateKey`). All channel URLs/endpoint on `hooks.example.com` (in the MockTransport allowlist); ConfigJson is PLAINTEXT (`_decrypt_notification_config` is identity on un-prefixed values). The push subscriber `p256dh` is a fixed 65-byte uncompressed P-256 point + 16-byte `auth`, so the RFC8291 crypto runs without raising. 2 new upstream fixtures (slack 200, push 201; webhook reuses the existing `…/ops` 200). 5 routes: `…_test__webhook`/`…_slack`/`…_push` → `{"ok": true}` (200), `…_unknown` → `{"ok": false, "error": "Unknown channel type: bogus"}` (500), `…_fail` → `{"ok": false, "error": "Webhook returned HTTP 404"}` (500). **Email/SMTP branch (`_dispatch_email_channel` ~24858, via `asyncio.to_thread`) DEFERRED** — no HTTP mock; would need a stub SMTP server. Scoped GREEN 5 / RED 0 / MISSING_GOLDEN 0. |
| R33 | **COVERAGE-ONLY, NO Go fix.** ai_helper **tool-call** cluster (`POST /api/ai/helper` → the NON-streaming `ai_helper` turn loop, app.py 28131-28279). The existing `aihelper`/`aihelpermem` profiles only ever exercised the no-tool answer/memory paths, so the entire tool branch was dark: `_extract_stream_tool_call_deltas` (app.py 4553 — the SSE `tool_calls` delta accumulator), `_normalize_generic_ui_action_tool_call` (app.py 4399, the single biggest target ~56 lines), its `apply_sql_filter` `sql_where`-extraction sub-branch (4471-4494), `_build_client_action` sanitization (4341), `_issue_ai_action_token` (4317) reached from the tool loop, and the loop's continue/grow-context + `max_tool_rounds` cap arms (28257-28279). | The byte-compared response is deterministic: the canned MAIN `/chat/completions` SSE (URL-keyed mock, request-body-ignored) emits ONE `tool_calls` delta carrying `function propose_ui_action` with `action_id=logs.filter.apply_sql` (which EXISTS in the `/logs` action manifest, parsed from `templates/logs.html`). The action_token is a SHA256-over-base64 signature of the action payload + the FIXED `SOBS_SECRET_KEY` with `iat/exp` from the FROZEN clock (`time.time()`→FIXED_EPOCH; Go reads the same via `SOBS_FAKE_EPOCH`), so it is byte-identical on both sides. Because `logs.filter.apply_sql` is on-page (`requires_confirmation` False), the loop never early-breaks on pending confirmation and the URL-keyed mock returns the SAME tool-call SSE every round → the loop runs to its `max_tool_rounds` cap (4 identical normalized proposals, answer-content accumulated 4×). No memory_candidates → `saved_memory_ids: []` (NO mask needed). The Go port (`handleApiAiHelper` non-streaming tool loop, `streamLLMEndpoint`/`extractStreamToolCallDeltas`, `normalizeGenericUIActionToolCall`, `buildClientAction`, `issueAiActionToken`/`attachAiActionToken` in `ai_helper.go`+`ai_action_normalize.go`) was ALREADY complete and byte-faithful — both runtimes return the identical 4-proposal turn with matching action tokens — so this is coverage, not a fix. | NEW `aitools` pure-env profile (clone of `aihelper`; distinct `sobs-ai.mock/aitools/v1` + `/aitools-guard/v1` paths) + 2 NEW upstream fixtures (the tool-call SSE on the main path; the `"safe"` guard JSON). Route `post__api_ai_helper_tools` (`/logs`, question "Apply a SQL filter to show only error logs.", fixed `chat_id`/`turn_id` so no CSPRNG ids leak; no mask). Scoped GREEN 1 / RED 0, STABLE across two full re-capture+re-check runs (golden bytes identical). **Residual uncovered (classified, not chased):** the SSE STREAMING `_generate` generator (app.py 27807-28123) is NOT covered — the Go `handleApiAiHelper` implements only the JSON path (it is never `stream_requested`), so covering the streaming branch is a Go-port task, not a fixture task → SCHEDULABLE (large). The reasoning/thinking-delta render arms → SCHEDULABLE (needs a thinking-capable model name + reasoning deltas; the JSON path emits no per-delta events). The unsupported-action / apply_form_filters allowlist-reject / cross-page-confirmation early-break arms of `_normalize_generic_ui_action_tool_call` → SCHEDULABLE (different action_id/page seeds). The `_suggest_chart_dashboard_pivot_tool` fallback → covered indirectly only when NO tool fires; SCHEDULABLE. The turn.cancelled / turn.error stream arms → DEFENSIVE. |
| R26 | **COVERAGE-ONLY, NO Go fix.** RUM ingest verification + remap cluster on `POST /v1/rum` (`ingest_rum` app.py 10010). TWO previously-dark chains: (A) `_verify_rum_client_auth` (app.py 7827-7878) + `_rum_client_token_decode` (7810) — the prior corpus only ran mode `none` (immediate pass-through 10025), so the whole enabled body (mode validation, signing-key gate, token extraction from header/`clientAuthToken`, HMAC decode, `exp`/origin/app checks) was unverified; (B) the source-map remap chain `_maybe_demangle_js_stack` (8030) -> `_remap_rum_console_stacks` (8064) -> `_sourcemap_lookup_for_file` (7970) reached at 10037-10038 — gated on `SOURCE_MAP_ENABLE` + a real `SOURCE_MAP_DIR`, never enabled in capture, so the `.map` load + VLQ lookup + frame rewrite were dark. | The `ingest_rum` response is the constant `{"accepted": <count>}` (a pure seeded event count): the remapped stack + auth claims go into the STORED `Body`/`exception.stacktrace` rows, never the response. So byte-parity verifies that BOTH runtimes reach the SAME accept/reject DECISION (200 accepted:N vs 401 {"error":...}) — and on the success path both actually run the source-map remap into the stored row. The Go ports were ALREADY complete and byte-faithful: auth in `rum_client.go` (`verifyRumClientAuth`/`rumClientTokenDecode`, HMAC-SHA256 hex + b64url RawURLEncoding + constant-time compare + exp/origin/app), and the sourcemap in `source_map.go` (`demangleStack`/`lookupForFile`/`parseSourceMap` — a stdlib base64-VLQ decoder whose `lookup` documents and mirrors the `sourcemap` PyPI lib's bisect-right/source-less-token semantics verbatim). Verified independently: Python `sourcemap.loads(map).lookup(0,4)` and the Go decoder BOTH yield `src.js:0:0 name=myFunc` (both +1 -> `src.js:1:1`), and `lookup(0,0)` raises IndexError on both -> no remap. So coverage, not a fix. | NEW env-only `rumauthmap` profile (`SOBS_RUM_CLIENT_AUTH_MODE=origin` + `SOBS_RUM_CLIENT_SIGNING_KEY=parity-rum-signing-key`, same key as `rumtoken`; `SOBS_SOURCE_MAP_ENABLE=1` + `SOBS_SOURCE_MAP_DIR=` the absolute `migration/fixtures/sourcemaps` computed in `profiles.py` so the path resolves identically from the in-process Python capture and the cwd=REPO Go server) + 1 committed fixture `migration/fixtures/sourcemaps/app.min.js.map` (`mappings:IAAAA` -> `app.min.js:1:5` resolves to `myFunc (src.js:1:1)`). 4 routes, tokens pre-signed OFFLINE with the profile key (success `exp=4102444800`=year 2100 so the expiry check is wall-clock-independent — no determinism anchoring): `post__v1_rum__authmap_ok` (header token + matching Origin + an error event whose top-level `stack` AND `breadcrumbs.console[].stack` reference `app.min.js:1:5`, plus a 2nd event using the per-event `clientAuthToken` fallback) -> 200 `{"accepted":2}`; `…_missing` -> 401 `{"error":"Missing RUM client auth token"}`; `…_expired` (past-exp token) -> 401 `{"error":"RUM client token expired"}`; `…_origin_mismatch` (token bound to other.example.com) -> 401 `{"error":"RUM client token origin mismatch"}`. NO masks (responses are constant). Scoped GREEN 4 / RED 0 / MISSING_GOLDEN 0; full Docker barrier after: **GREEN 655 / RED 0 / MISSING_GOLDEN 0 / EXCLUDED 1** (no regression). **Residual arms classified (schedulable, not blockers):** invalid-mode (500), no-signing-key (503), token-format/signature/payload decode errors (401), missing-origin-binding & missing-request-Origin (401), and app-mismatch (401) are each one more cheap route (different token/header/profile) — deferred only to keep this milestone tight, no harness obstacle. Sourcemap NO-MATCH / file-missing / `sourcemap`-import-fail / source-less-token-fallback arms are defensive or covered by Go unit tests (`source_map_test.go`). |
| R27 | **GO FIX.** Notification-rule EVALUATION cluster on `POST /api/notifications/check` (`check_notifications` app.py 26341 → `_check_notification_rule` 25185 / `_evaluate_tag_condition` 25127 / `_normalize_notification_condition` 24626). A FIRING tag rule's dispatch summary diverged at byte 267: golden (Python) `… eq production > 0.0 (value=3.0)` vs Go `… eq production > 0 (value=3.0)` — the rule's `threshold` rendered as `0` in Go but `0.0` in Python (4-byte body diff; both 200). | Python loads rules via `_load_notification_rules` → `_parse_notification_conditions_json` → **`_normalize_notification_condition`** (24626), which rebuilds each condition dict coercing `threshold = float(raw.get("threshold") or 0)`. So `cond['threshold']` is a Python **float** (`0.0`), and `_build_notification_payload`'s summary f-string `f"… {cond.get('threshold', 0)} …"` renders `0.0`. The Go `loadNotificationRulesForCheck` (`notif_check.go`) instead used the **RAW** parsed JSON conditions (`parseJSONValue` → the int `0`), skipping normalization entirely — so `condThreshold`/`pyStrAny` emitted the raw `0`. (Latent: every prior `/api/notifications/check` golden — `notifcheck`/`notifrule`/`notifagent` — seeded rules with EMPTY `ConditionsJson` `"[]"`, so the evaluator never built a fired-condition summary and the un-normalized threshold was never byte-compared. A faithful `normalizeNotificationCondition` returning `map[string]any` ALREADY existed in `fix_pages_settings.go` for the settings page, but the check handler never called it.) | added `normalizeNotificationConditionObj(raw any) *jsonenc.Object` in `notif_check.go` — a mirror of `_normalize_notification_condition` that returns a `*jsonenc.Object` with keys in Python's exact dict-literal ORDER (so the downstream masked-conditions payload / `ConditionsJson` write-back are byte-faithful) and `threshold` as a `float64` (→ `toStr`/`PyFloatRepr` renders `0.0`, `99.0`). Reuses the existing `notificationComparators`/`notificationTagMatchOperators`/`tagRuleRecordTypes` validity maps + `condFloat`/`condWindowMinutes`/`orDefault` helpers (same defaulting as the settings-page normalizer). `loadNotificationRulesForCheck` now runs each parsed entry through it (dropping non-object entries, like Python's `None` return), so the evaluator + summary see the normalized shape. Downstream is unchanged: `cloneObject(cond).Set("_value", …)` keeps the normalized-then-`_value` key order; `evaluateTagCondition`/`evaluateSignalCondition` read the same fields. Found + covered by the NEW `notifeval` profile (`seed_notifeval`) + `post__api_notifications_check__eval` route. **Seed = 5 env tags (3 production/log + 1 production/trace + 1 staging/log) at frozen-era Version 1704164644000 + 1 webhook channel on `hooks.example.com/ops` (canned 200) + 4 rules, one per evaluator arm:** A "Aaa Prod Tag Fires" (logic=any, tag eq production gt 0 → count 3 > 0 → FIRES → dispatch ok → log/LastFiredAt writes), B "Bbb Defaults NoFire" (logic=all: a tag cond with INVALID record_type/operator/comparator → all/eq/gt defaults + empty value → count 5, `5 > 99` false; AND a signal cond over the now()-windowed empty fixture → no row → False → "conditions not met"), C "Ccc Disabled" (enabled=0 → "disabled", its regex/lt/window-999 cond normalized at load), D "Ddd Cooldown" (LastFiredAt 5s before frozen now + cooldown 300 → "cooldown", its contains/gte/window-0 cond normalized at load). **Determinism:** tag counts use `_evaluate_tag_condition`'s FROZEN-time `min_version` (`(time.time()-window*60)*1000`, both runtimes via SOBS_FAKE_EPOCH) so the count is a constant; cooldown compares FROZEN `now()` vs the stored `LastFiredAt` literal; the lone signal cond windows on un-frozen chdb `now()` over a fixture with no in-window logs → no row regardless of wall clock. The response carries NO uuid/timestamp (the log-row uuid + `fired_at` live only in DB writes / the mock-ignored outbound body) → byte-compared with **NO masks**. AI unconfigured → `agent_runs == []`. Scoped GREEN 1 / RED 0; full Docker barrier after the fix: **GREEN 656 / RED 0 / MISSING_GOLDEN 0 / EXCLUDED 1** (no regression). **Residual arms classified (schedulable, not blockers):** the eq/lt/gte/lte comparator true-arms of the evaluators, the signal-condition POPULATED match (needs now()-anchored logs like `seed_summaryrich`), the `contains`/`regex` tag value-filter true-arms, the per-rule try/except `"rule evaluation failed"` arm (DEFENSIVE), the channel-not-found / channel-disabled dispatch arms, and the `mask_output_enabled=false` payload branch are each one more cheap rule/channel/cond — deferred to keep this milestone tight, no harness obstacle. |
| R28 | **COVERAGE-ONLY, NO Go fix.** AI guard-model cluster on `POST /api/query/ask` (`api_query_ask` app.py 30203 → `_check_guard_model` 5136 → **`_call_llm_endpoint` 4615, the NON-streaming OpenAI-compatible /chat/completions call used by guard, agent-flow, issue-dedupe, memory** → `_parse_oss_safeguard_reply` 5042 / `_parse_guard_reply` 5009). The **guard-BLOCK** path was dark: the only prior `/api/query/ask` golden (`post__api_query_ask__llm`, profile `ask`) returns a plain `"SAFE"` guard reply → ALLOW → NL→SQL; the BLOCK branch (app.py 30266-30277, the 403 `{ok:false, error:"Request blocked by safety guard: {guard_reason}", trace_id, turn_id}`) and BOTH verdict parsers' UNSAFE arms were never byte-compared. | The 403 response carries only `ok`/`error`/`trace_id`/`turn_id`. `trace_id = md5("query\|{question}\|{time_ns}")` and `turn_id = trace_id[:16]` are TIME-BASED (`time.time_ns()`) → MASKED (`"trace_id": ?"[0-9a-f]{32}"`, `"turn_id": ?"[0-9a-f]{16}"`; the mask requires the hex present so an absent id would diff RED). The deterministic guard reason (carried in `error`) IS byte-compared, and `_call_llm_endpoint` records latency in `stats` which is NOT in this response (no leak). The Go ports — `handleApiQueryAsk` (`query_exec.go`) → `checkGuardModel` → `aiHelperGuardCheck` (`ai_helper.go`) → `guardCallLLM` (the full non-streaming `_call_llm_endpoint` port with the empty-content retry, `fix_ai_helper.go`) → `parseOssSafeguardReply`/`parseGuardReplyFull` (`fix_ai_helper.go`), with `isGptOssSafeguardModel` routing + `aiGuardCategoryLabel`/`aiGuardNoisyCategories` + the benign-override arms + the exact `blocked (Sx: Label)` reason formatting — were ALREADY complete and byte-faithful on both parser paths. So coverage, not a fix. | TWO NEW env-only profiles (no DB seed — `ai.*` come from profile env, like `ask`) + 2 canned guard replies on `sobs-ai.mock`. **`aiguard`** (`SOBS_AI_GUARD_MODEL=gpt-oss-safeguard-20b` → `_is_gpt_oss_safeguard_model` true → `_build_oss_safeguard_prompt` + `_parse_oss_safeguard_reply`): guard fixture content is a JSON verdict `{"violation":1,"policy_category":"S7",...}` → UNSAFE + S7. **`aiguardllama`** (`SOBS_AI_GUARD_MODEL=llama-guard-3-8b` → the OTHER branch → `_build_llama_guard_prompt` + `_parse_guard_reply`): fixture content `"unsafe\nS9"` → UNSAFE + S9. Both categories (S7 Privacy, S9 Indiscriminate Weapons) are deliberately NON-noisy (not in `_AI_GUARD_NOISY_CATEGORIES`), so NONE of the benign-override arms apply → clean deterministic blocks. The main `ai.endpoint_url`/`ai.model` are set only to pass `_query_page_enabled`; the block path returns before dialing them. Question `"how many logs are there"` passes `_heuristic_guard_check` (no block keywords). Routes `post__api_query_ask__guardblock` (golden `403 {"error":"Request blocked by safety guard: blocked (S7: Privacy)","ok":false,"trace_id":<32hex>,"turn_id":<16hex>}`) + `post__api_query_ask__guardllama` (golden `… blocked (S9: Indiscriminate Weapons) …`). The whole non-streaming LLM chain (request build → POST `/chat/completions` → response parse → content extraction → verdict parse → category→reason) runs on the canned reply; GREEN proves it ran identically on both sides. Scoped GREEN 2 / RED 0 / MISSING_GOLDEN 0. **Residual arms classified (schedulable, not blockers):** the guard ALLOW path (already covered by `post__api_query_ask__llm`); `_call_llm_endpoint`'s empty-content-retry branch (needs a canned EMPTY-content reply, then a second reply — schedulable); the benign-override arms (need a noisy-category verdict + a benign-observability/ai-usage/navigation question — schedulable, ~3 cheap routes); the heuristic-block early-return (a question containing a block keyword → `"Blocked by heuristic safety check"` — cheap, schedulable); `guard_not_configured`/`guard_unavailable`/`guard_invalid_reply` and the LLM error/timeout branches → DEFENSIVE/deferred. |
| R29 | **COVERAGE-ONLY, NO Go fix.** Full agent flow CREATE-NEW-ISSUE arm of `_run_agent_flow` (app.py 6772-6999) reached via `POST /api/agent/runs` (`trigger_agent_run` 28671 → `_run_agent_rule_instance` 25426 → `_run_agent_flow`). The prior `agenttrigger` golden seeds an **analyze-only** rule, so `_run_agent_flow`'s github-issue arm (6864-6965: `_resolve_agent_github_target`, the issue-title/body build, `_choose_github_issue_outcome` 5388, `_create_github_issue_record` 6380, `_persist_github_work_item`, the `dedup_decision`/`copilot_*`/`github_issue_url` result fields) was UNVERIFIED *via this route* — the only other create golden (`issuesraise`) reaches the same flow through a DIFFERENT entrypoint (`raise_issue_from_user_observation`). The trigger_agent_run → analyze+github_issue → create-new path was dark. | The response is `{ok, run_id, result{status, guard_decision, dlp_result, analysis, suggestion, github_issue_url, dedup_decision, issue_error, copilot_assignment_*}}`. Only `run_id` (= `str(uuid.uuid4())` in `_run_agent_rule_instance`) is volatile → MASKED (`"run_id": ?"[^"]*"`). The wall-clock `Version=int(time.time()*1000)` and `CreatedAt`/`CompletedAt` go ONLY into the `sobs_agent_runs`/`sobs_github_work_items` DB rows, never the response. The canned analysis/suggestion (from the agent-LLM mock) + `github_issue_url` (from the create mock) + `guard_decision`/`dlp_result`/`dedup_decision`/`status` are all deterministic and byte-compared. The Go ports — `handleTriggerAgentRun` → `runAgentRuleInstance` → `runAgentFlow` → `chooseGithubIssueOutcome` → `createGithubIssueRecord` (`agent_flow.go`/`agent_dedupe.go`) — were ALREADY complete and byte-faithful (the create-new path was already exercised, just through `handleApiIssuesRaise`); both runtimes emitted the identical `agent_issue_decision_summary` (only the uuid differs). So coverage, not a fix. | NEW `agentflow` profile (`seed_agentflow`: one enabled `manual`-trigger rule `e3…0001` with `Actions="analyze,github_issue"` + global `ai.github_repo=acme/widget`/`ai.github_token=ghp_parity_token`) reusing the SAME canned fixtures as `issuesraise` (guard `SAFE`, analyze `ROOT CAUSE`/`SUGGESTED FIX`, create `POST /repos/acme/widget/issues`→#42). The open-issues GET `…/issues?state=open&per_page=10` 404s (no fixture) → no dedup candidate → `_classify_issue_dedupe_with_llm` fallback "unrelated" → `allow_new_issue` (no prior runs → `_count_github_issues_last_hour()`=0 < max) → fresh issue created. `manual` trigger → no service_name → `_resolve_agent_github_target` falls to the global repo. Route `post__api_agent_runs__flow` (mask `run_id`); golden = `{ok:true, result:{analysis:"Connection pool exhausted…", suggestion:"Raise the pool ceiling…", guard_decision:"allowed", dlp_result:"skipped", github_issue_url:".../issues/42", dedup_decision:"new_issue", copilot_assignment_status:"not_requested", issue_error:"", status:"completed"}}`. Scoped GREEN 1 / RED 0 / MISSING_GOLDEN 0. **Residual arms classified (schedulable, not blockers):** the dedupe-reuse-existing arm (`reused_existing`/`related_existing` — covered for the OTHER route by `issuereuse`; schedulable for trigger_agent_run with a prior work item + matching open-issue fixture); the DLP-block arm (6874-6890, needs `dlp_check` action + a canned FLAGGED DLP verdict → `status:completed`, no issue — schedulable); the rate-limit-exceeded arm (6892-6893, `allow_new_issue=False` → `suppressed_rate_limit`, needs >max prior in-window runs — schedulable); the copilot-assignment arm (`github_issue_copilot` action → `_assign_issue_to_copilot` — schedulable); the guard-BLOCK arm (6813-6821 `blocked_by_guard`, needs a canned UNSAFE guard reply — schedulable); the `_run_agent_rule_instance` exception arm (25461-25488 `status:failed`) → DEFENSIVE. |
| R30 | **COVERAGE-ONLY, NO Go fix.** Dependency lockfile-parser cluster on `POST /api/enrichment/cve/scan` (`_run_cve_scan` → `_fetch_release_deps_from_github` app.py 16703 → the contents-API loop 16794 → the four pure parsers `_parse_requirements_dependencies` 16333, `_parse_package_lock_dependencies` 16358, `_parse_go_sum_dependencies` 16402, `_parse_gemfile_lock_dependencies` 16426). The prior `depsrich` golden seeds ONE release whose repo serves a MODERN `packages`-keyed package-lock.json — so only the package-lock modern branch was byte-exercised. The requirements/go.sum/Gemfile.lock parsers AND the package-lock LEGACY top-level `dependencies` branch (16385-16398) were dark (~86 lines). | The cve-scan summary response carries only the COUNTS (`github_backfill_attempted`/`_inserted`/`_max_releases`, `libraries_found`, `vulns_found`, `ok`, `scanned_at`); the parsed deps go into the `sobs_release_artifacts.MetadataJson` DB rows, NOT the response. So a parser DIVERGENCE surfaces as a wrong `inserted`/`libraries_found`/`vulns_found` COUNT. The contents loop BREAKS after the first lockfile that returns 200 AND parses non-empty, so ONE release covers ONE parser → seeded FOUR enabled apps/releases (distinct repos, all `ReleaseVersion=1.0.0`+`CommitSha`) so the backfill iterates all four and hits a distinct parser each. `scanned_at` is `_now_iso()` (wall-clock) → MASKED; every count is deterministic. The Go ports — `parseRequirementsDeps`, `parseGoSumDeps`, `parseGemfileLockDeps`, `parsePackageLockDeps` legacy branch (`cve_scan.go`) reached via `githubContentsLockfileRows` (Go's `githubActionsDependencyRows` is a nil-stub, so BOTH runtimes skip the actions traversal and fall to contents identically) — were ALREADY byte-faithful: scoped parity GREEN on the first run, all four `inserted=4`/`libraries_found=13`/`vulns_found=13` counts identical. So coverage, not a fix. | NEW `lockfiles` profile (`seed_lockfiles`: 4 apps `acme/reqs`,`acme/npm`,`acme/gomod`,`acme/ruby` + 4 releases + shared `ai.github_token`) reusing the SAME canned OSV fixture as `depsrich` (`POST api.osv.dev/v1/query` is body-keyed-but-URL-only → one fixture serves all 13 libs → 1 vuln each → `vulns_found=13`). FOUR new contents fixtures (base64 lockfiles): `acme/reqs/requirements.txt`→4 PyPI (`flask`,`Requests`,`django`,`pytest`; exercises blank/`#`-comment/inline-` #`/`;`-marker/no-`==`/empty-name/empty-ver/case-insensitive-dedup arms), `acme/npm/package-lock.json`→3 npm via the LEGACY top-level `dependencies` obj (NO `packages` key → modern branch empty → legacy; `express`,`lodash`,`chalk`; exercises missing-version-skip, non-dict-entry-skip, case-insensitive dedup), `acme/gomod/go.sum`→3 Go (`github.com/pkg/errors`,`golang.org/x/sys`,`github.com/stretchr/testify`; exercises blank-skip, <2-field-skip, `/go.mod`-suffix-strip+dedup), `acme/ruby/Gemfile.lock`→3 RubyGems (`rails`,`nokogiri`,`rack`; exercises pre-`specs:` skip, indented `  specs:` marker, in-specs comment-skip, `,`-constraint→first-version, non-indented-line-break, case-insensitive dedup). Earlier-candidate lockfiles + `actions/runs` 404 via the harness missing-fixture→404 default (both runtimes 404 identically) — no 404 fixture files needed. Route `post__api_enrichment_cve_scan__lockfiles` (mask `"scanned_at"`); golden = `{github_backfill_attempted:4,github_backfill_inserted:4,github_backfill_max_releases:300,libraries_found:13,ok:true,vulns_found:13}`. Scoped GREEN 1 / RED 0 / MISSING_GOLDEN 0. **Residual arms classified (schedulable/defensive, not blockers):** the package-lock MODERN `packages` branch (already covered by `depsrich`); each parser's `if not isinstance(body,dict)`/non-dict-content early guards + the `_decode_github_contents_payload` non-base64/decode-error branches (DEFENSIVE — unreachable with well-formed canned fixtures); the contents-loop `resp.status_code != 200 → break` arm (needs a canned non-200/non-404 lockfile response — schedulable); the GH-Actions snapshot-artifact zip path (16544-16627, binary zip not representable in the utf-8 `content` fixture harness, and Go's `githubActionsDependencyRows` is a deliberate nil-stub) → deferred. |
| R31 | **COVERAGE-ONLY, NO Go fix.** GitHub Copilot ISSUE-ASSIGNMENT arm of the agent flow, reached via `POST /api/agent/runs` (`trigger_agent_run` -> `_run_agent_rule_instance` -> `_run_agent_flow` app.py 6772) when the triggered rule's `Actions` contains `github_issue_copilot` (-> `wants_copilot_assignment=True`, app.py 6865). After the create-new-issue arm (`_choose_github_issue_outcome` app.py 5388, create branch ~5510-5552) makes issue #42, it runs the Copilot-support probe `_github_repo_supports_copilot_assignment` (app.py 5280, a GraphQL `suggestedActors` POST to `api.github.com/graphql`) then `_assign_issue_to_copilot` (app.py 5329, a POST to `/repos/{owner}/{repo}/issues/{n}/assignees`). The prior `agentflow` golden (M29/R29) seeded an `analyze,github_issue` rule, so `wants_copilot_assignment` was always False and this whole arm (both copilot HTTP calls + the assignee-membership check + the `requested`/accepted-but-lagging status strings + the create-branch copilot rate limiters at 5532/5535) was dark. | The agent-run RESPONSE returns only `copilot_assignment_status` (`"requested"`) and `copilot_assignment_reason` (`"Copilot assignment requested"`) for this arm — BOTH deterministic and byte-compared. The `requested_at` (`int(time.time()*1000)`, wall-clock) is NOT in the response — it lives ONLY in the `sobs_github_work_items`/`sobs_agent_runs` DB rows (via `_persist_github_work_item`), so NO timestamp mask is needed; only `run_id` (uuid4) is masked, exactly as `agentflow`. With zero prior work items both copilot limiters (`_count_copilot_assignments_last_hour`=0, `_count_active_copilot_assignments`=0) are below the default cap of 1 (`_AI_AGENT_MAX_ASSIGNMENTS_PER_HOUR_DEFAULT`/`_AI_AGENT_MAX_ACTIVE_ASSIGNMENTS_DEFAULT` both = 1), so the assignment deterministically proceeds. The Go ports — `runAgentFlow`->`chooseGithubIssueOutcome` create branch (`agent_dedupe.go`)-> `assignIssueToCopilot` + `githubRepoSupportsCopilot` (`onboarding_issues.go`/`github_inspect.go`) — were ALREADY complete and byte-faithful: scoped parity was GREEN on the FIRST capture once the assignees fixture was in place (both runtimes emitted identical `agent_issue_decision_summary` with `copilot_assignment_status:"requested"`; only the uuid differs). So coverage, not a fix. | NEW `agentcopilot` profile (`seed_agentcopilot`: one enabled `manual`-trigger rule `e3…0002` with `Actions="analyze,github_issue_copilot"` + global `ai.github_repo=acme/widget`/`ai.github_token=ghp_parity_token`) reusing the SAME canned fixtures as `agentflow`/`issuesraise` (guard `SAFE`, analyze `ROOT CAUSE`/`SUGGESTED FIX`, create `POST /repos/acme/widget/issues`->#42, and the host-keyed GraphQL `POST api.github.com/graphql` whose `suggestedActors.nodes` already carries `copilot-swe-agent` -> supported). ONE new fixture: the assignees `POST /repos/acme/widget/issues/42/assignees` (key `ff163be01ed0a93eb1800139208d2fed`) returning the issue with `assignees:[{login:"copilot-swe-agent[bot]"}]` so the membership check passes -> primary `"requested"`/`"Copilot assignment requested"` arm (NOT the accepted-but-lagging arm). The open-issues GET 404s -> no dedup candidate -> fresh issue. Route `post__api_agent_runs__copilot` (profile `agentcopilot`, POST `{rule_id:e3…0002}`, mask `run_id`); golden = `{ok:true, result:{analysis:"Connection pool exhausted…", suggestion:"Raise the pool ceiling…", guard_decision:"allowed", dlp_result:"skipped", github_issue_url:".../issues/42", dedup_decision:"new_issue", copilot_assignment_status:"requested", copilot_assignment_reason:"Copilot assignment requested", issue_error:"", status:"completed"}}`. Scoped GREEN 1 / RED 0 / MISSING_GOLDEN 0. **Residual arms classified (schedulable/defensive, not blockers):** the accepted-but-lagging-visibility arm (5374-5378, needs an assignees response WITHOUT `copilot-swe-agent[bot]` -> `"…assignee visibility may lag briefly"` — schedulable, ~1 route + 1 fixture); the support-probe UNSUPPORTED arm (`_github_repo_supports_copilot_assignment` returns False -> `"blocked","Copilot cloud agent is not enabled…"`, needs a GraphQL fixture with no copilot actor — schedulable); the create-branch copilot rate-limit-blocked arms (5532/5535, need prior in-window/active work items — schedulable); the reuse-branch copilot path (5510-5552 reuse arm; the `issuereuse` profile already covers its BLOCKED variant) — schedulable; the assignees-POST HTTP-error/`failed` arm (5379-5385) and the support-probe exception arm -> DEFENSIVE. |
| R32 | **COVERAGE-ONLY, NO Go fix.** SEASONAL (per-bucket) metric-rule candidate generator on `POST /metrics/rules/auto` with `mode=seasonal` (`auto_metrics_rules` app.py 13746 -> `_build_seasonal_metric_rule_candidates` 11845, ~52 lines, the biggest non-AI coverable function). The prior `metricsauto`/`metricscreate` goldens only drive `mode=threshold` (`_build_auto_metric_rule_candidates`), so the seasonal builder — the per-series-per-bucket quantile scan (`quantile(…)(toFloat64(value))` grouped by `_build_seasonal_bucket_expr(strategy)` = `toHour(time)`/`toDayOfWeek(time)`), the `_SEASONAL_MIN_BUCKET_POINTS=3` per-bucket HAVING gate, the eligible-series IN-subquery, the `bucket_index` build (`_infer_auto_rule_comparator` + `_auto_rule_thresholds` per bucket), and the seasonal candidate assembly (`seasonal_buckets_json`/`seasonal_bucket_count`/`seasonal_strategy`) — was entirely dark. | The `action=preview` render (`templates/metrics_rules.html` 200-225) emits, per candidate: name/source/signal/service/attr_fp, the seasonal badge title (`seasonal_strategy` + `seasonal_bucket_count` "bucket(s)"), comparator, `warning_threshold`/`critical_threshold`, `min_sample_count`, `point_count` — ALL deterministic from a constant series. **Crucially the wall-clock bucket KEY (the `toHour()` value 0-23) lives ONLY in `seasonal_buckets_json`, which the preview does NOT render** (it is rendered only for PERSISTED rules, template line 534); the preview shows the bucket COUNT, not the key. So with a single-hour seed the page is byte-reproducible with **NO mask at all** (verified: two independent capture cycles seeded ~1 min apart produced a byte-identical body, sha `c352b142…`). The Go ports — `buildSeasonalMetricRuleCandidates` (`metric_candidates.go`, incl. the eligible-series subquery, the ordered `jsonenc.Object` bucket index, `inferAutoRuleComparator`/`autoRuleThresholds`, and the `handlers_pages.go` seasonal-mode dispatch) — were ALREADY complete and byte-faithful: scoped parity GREEN on the FIRST capture, all three candidates' float thresholds (`5.0`/`5.5`, `0.0`/`0.1` via `PyFloatRepr`) and integer `point_count`/`seasonal_bucket_count` identical. So coverage, not a fix. (The stale `metric_candidates.go:160` note "not byte-reproducible across capture/replay … never by the parity oracle" is now superseded by this single-hour-anchor technique; left as-is since it still describes the *general* multi-hour case.) | NEW `seasonalauto` profile (`seed_seasonalauto`: 155 otel_logs rows for the EXISTING base service `web`, laid out as 5 logs in each of 31 distinct minute buckets anchored to `toStartOfHour(now()) + INTERVAL m MINUTE`, m=0..30). **Determinism, layered:** (1) CONSTANT value — 5 logs/minute-bucket -> the 1m derived view yields a CONSTANT `log_volume=5` series (error_volume/error_ratio constant 0), so every reservoir `quantile(q05..q95)` equals the constant EXACTLY despite chDB's otherwise non-deterministic sampler -> fixed thresholds (gt log_volume warning q80=5.0 / critical q95==q80->`warning*1.1`=5.5; error_volume,error_ratio 0.0/0.1). (2) SINGLE wall-clock-stable bucket — anchoring every minute bucket inside ONE hour via `toStartOfHour(now())` makes all 31 buckets share one `toHour()`, so `seasonal_bucket_count` is ALWAYS exactly 1 regardless of the real hour (the differing hour NUMBER lives only in the un-rendered `seasonal_buckets_json`). `toStartOfHour(now()) <= now()` and the unbounded upper window (`time >= now() - INTERVAL hours HOUR`) keeps all 31 buckets in-scan at capture AND replay. (3) Per-bucket gate — the lone bucket holds 31 points >= `_SEASONAL_MIN_BUCKET_POINTS`=3; the route's `min_points=5` keeps the seed small (series point_count 31 >= 5). (4) Racy-UNION avoidance — reuse the lone base service `web` so `_list_derived_signal_dimensions`' DISTINCT-over-UNION services dropdown (trailing ORDER BY binds only to the last branch -> genuinely racy once >1 service) stays trivially deterministic. Route `post__metrics_rules_auto__seasonal` (profile `seasonalauto`, form `mode=seasonal,seasonal_strategy=hour_of_day,hours=24,min_points=5`, action defaults to preview, **NO mask**); golden = Preview Candidates (3): `Auto logs/error_ratio [web]` (gt 0.0/0.1, 1 bucket, 31 pts), `Auto logs/error_volume [web]` (gt 0.0/0.1, 1 bucket, 31 pts), `Auto logs/log_volume [web]` (gt 5.0/5.5, 1 bucket, 31 pts); summary "Examined: 3 · Candidates: 3 · Existing skipped: 0 · Invalid skipped: 0". Scoped GREEN 1 / RED 0 / MISSING_GOLDEN 0, byte-stable across two independent runs. **Residual arms classified (schedulable/defensive, not blockers):** the `day_of_week` strategy (`toDayOfWeek(time)` — schedulable: a seed spanning >=3 distinct same-weekday days, or anchored to one weekday, with the count-only render keeping it deterministic); the `include_attr_fp=1` branch (`AttrFingerprint` select/group — schedulable, one form field + the same constant series); the `service_filter` WHERE arm (schedulable, one form field); the no-eligible-series early outcome (empty `series_rows` -> 0 candidates — schedulable via an empty-window `hours` or below-`min_points` seed, same as the threshold empty-window route); the `skipped_existing` arm (a pre-seeded `sobs_anomaly_rules` row with `RuleType=seasonal` matching a candidate scope — schedulable); the `skipped_invalid` arms (comparator gt critical<warning / lt critical>warning — need a NON-constant series whose quantiles invert, which reintroduces reservoir non-determinism, so DEFENSIVE/deferred); the `action=create` path (inserts `sobs_anomaly_rules` rows incl. `SeasonalBucketsJson` — the create logic mirrors the threshold create already covered by `metricscreate`; the persisted-rule render of `seasonal_buckets_json` carries the wall-clock bucket key so a follow-up GET would need a key mask — schedulable). |
| R34 | **GENUINE Go FIX.** `require_basic_auth` decorator branches (app.py 7888-7941 `decorated` + `_auth_mode` 7581 + `_check_external_auth` 7561 + `_same_origin_request` 7781). The base corpus carries no auth env, so only the `mode=="none"` arm ran; the basic/external/invalid arms + the CSRF origin check + the 401 `WWW-Authenticate` realm challenge were dark (~32 lines). Driving them via env-only profiles exposed one genuine divergence on the **external-PASS** arm: Go `checkExternalAuth` POSTed to `{EXTERNAL_AUTH_URL}/internal/auth/validate` through its OWN dedicated `externalAuthClient = &http.Client{}` (`auth.go`), which does NOT honour the `SOBS_UPSTREAM_FIXTURES` mock. Under the parity harness the Python oracle's `_check_external_auth` dials via `_get_async_http_client()` → an `httpx.AsyncClient` that the determinism shim patches with a `MockTransport` (so it reads the canned 200 → PASS), whereas Go's raw client would hit the (non-existent) `sobs-ai.mock` host → connection error → False → 401. So Python returns 200 (handler runs) while Go returns 401 — a guaranteed RED on the external-valid-Bearer case. | Both the 401 `Unauthorized` body **and** the `WWW-Authenticate` realm header are byte-compared (`normalize.py` keeps `WWW-Authenticate`; it is in no drop-list), as are the 403 `{"error":"CSRF origin check failed"}` and 500 `{"error":"Server auth misconfiguration"}` JSON. The basic decode (`secrets.compare_digest` ↔ `subtle.ConstantTimeCompare`), CSRF `_same_origin_request` (no Origin/Referer → `""` ≠ any expected_origin → 403, host-independent), and the realm strings (`Basic realm="SOBS"` / `Bearer realm="SOBS"`) were ALREADY byte-faithful — only the external-dial wiring diverged. | **Go fix:** route `checkExternalAuth` through `s.upstreamRequest("POST", endpoint, nil, {"Authorization": authorization})` instead of the standalone `externalAuthClient` (`auth.go`). `upstreamRequest` honours the fixtures mock under the harness (matching the oracle's shimmed httpx client) and falls back to a real `http.Client` when `SOBS_UPSTREAM_FIXTURES` is unset (matching Python's runtime `_get_async_http_client`) — so the fix is faithful in BOTH harness and runtime. Removed the now-unused `externalAuthClient` var + `time` import. FOUR new env-only profiles (`profiles.py`, NOT in `SEEDED_PROFILES`): `authbasic` (basic creds + `SOBS_CSRF_ORIGIN_CHECK=1`), `authexternal` (`SOBS_EXTERNAL_AUTH_URL=http://sobs-ai.mock/extauth` + `SOBS_UPSTREAM_FIXTURES`, fixture `be138…json`→200), `authextfail` (`…/extauthbad`, fixture `463327…json`→403), `authinvalid` (basic creds AND external URL → mode invalid). EIGHT route variants on the already-GREEN GET `/api/web-traffic/browsers` (PASS cases) + POST `/api/data-management/prune` (CSRF case): `basic_ok`→200, `basic_badpass`→401 Basic, `basic_noauth`→401 Basic, `csrf_block`→403, `external_ok`→200, `external_nobearer`→401 Bearer, `external_reject`→401 Bearer, `invalid`→500. Scoped GREEN 8 / RED 0 / MISSING_GOLDEN 0. **Residual arms (schedulable/defensive, not blockers):** the external `session`-cookie fallback (`auth.startswith("Bearer ")` false → read `session` cookie → `"Bearer "+cookie`, app.py 7920-7924 — schedulable: a profile sending a `Cookie: session=…` header against the 200 fixture); the basic base64-decode-exception arm (`except Exception: pass`, app.py 7914 — DEFENSIVE, needs a malformed Basic blob and Python/Go decode-strictness happens to agree on well-formed input); the CSRF same-origin-PASS write (a POST WITH a matching `Origin` → falls through to the credential check — schedulable but host-dependent: capture host `localhost` vs replay host `127.0.0.1:PORT` differ, so the Origin must be set to whatever each side computes, which is NOT trivially shareable → deferred); the invalid-config single-cred sub-case (`SOBS_BASIC_AUTH_USERNAME` set without password → also mode invalid, same 500 body as the both-configured case already covered — schedulable, redundant body). |

## Deferred limitation

| # | Symptom | Root cause | Status |
|---|---|---|---|
| R12 | `auto_metrics_rules_dashboard` create redirects to `/dashboards/<id>` — Python emits a frozen sequential uuid (`00000000-0000-4000-8000-00000000000a`), Go emits a crypto-random one | Go's `newUUIDv4` is NOT parity-frozen (its own comment notes this); Python's `determinism.install()` makes `uuid.uuid4()` a sequential counter. Matching a **response-exposed** server-generated uuid would require Go to replicate Python's entire uuid-consumption sequence from boot (seeder + every prior route) — impractical and fragile. | **DEFERRED.** Any route that returns a server-generated uuid in its response body/Location (dashboard/chart create-and-redirect-to-id, etc.) is excluded from corpus parity. The create *logic* is still byte-unverifiable here; only the preview path is covered. Revisit only if a frozen-uuid lockstep is built into the Go parity harness. |
| R18 | `view_ai` cross-request divergence: after a route triggers a `totals=…` metadata error (e.g. `GET /ai?sql=nonexistent_col=1` → chdb UNKNOWN_IDENTIFIER on BOTH the COUNT and the totals SUM query, app.py 18747-18751 + 18955-18960), a *subsequent* same-process `/ai` request with the **same** `(from_ts,to_ts)` cache key renders the "Some AI metadata failed to load: totals=…" alert in the oracle but NOT in Go — the alert leaks into the next request's render. (The same-request render of the erroring route itself is byte-identical on both sides — that route is GREEN.) | The oracle's `_get_ai_filter_metadata` cache-MISS path returns the **same list object** it stored in `_ai_filter_metadata_cache[key]["errors"]`; `view_ai` then does `metadata_errors = metadata.get("errors")` and `metadata_errors.append("totals=…")` (app.py 18933 + 18956), **mutating the cached list in place**. The next cache-HIT request copies (`list(cached["errors"])`) the now-polluted list and renders the stale alert. Go's `getAiFilterMetadata` does not alias the cache into the handler's `metadataErrors` (the `append` produces a fresh slice), so it never replicates the leak. | **NOT a Go code change** (replicating an aliased-mutation cache bug would be fragile and is arguably an oracle defect). Resolved at the harness level: the only pollution source is the new `GET /ai?sql=nonexistent_col=1` route, so it lives in its OWN isolated capture profile (`airichsql`, distinct from `airich`). Per `capture_routes.py` each profile is captured in a separate process, so the mutation can never reach the plain-`airich` golden; both routes are GREEN. Revisit if a future route legitimately needs the totals-error path AND a same-process cache-hit follow-up — then a Go cache-alias shim would be required. |
| R20-z | `_github_actions_dependency_rows` GH-Actions snapshot **zip-download + unzip + pip-freeze parse** branch (app.py 16557-16624; Go `parseGithubActionsSnapshotZip` in `fix_cve_helpers.go`) — the part that GETs `archive_download_url` (Accept `application/octet-stream`), `zipfile.ZipFile(io.BytesIO(...))`, reads each `pip-freeze-<platform>-<arch>.txt`, and emits `source:"github_actions_artifact"` rows. Covered up to the artifact-name filter (`snapshot=None`), NOT into the zip. | The upstream-fixture harness serves a response body as either parsed `json` or a `content` **string**; the Python determinism shim does `str(spec["content"]).encode("utf-8")` and Go reads `RawContent` as `[]byte(string)`. A real zip's bytes are not valid-UTF-8-round-trippable, so the SAME zip cannot be delivered byte-identically to both runtimes via the current `content` field — the only divergence-free fixture body is one with no matching snapshot artifact (→ no download). | **DEFERRED (not representable, not a divergence).** The Go port of the zip path already exists and is byte-faithful to the oracle; it is simply unreachable under this harness. To cover it, add a base64-binary `content` mode to BOTH `determinism.py`'s shim and `upstream.go`'s `upstreamFixture` (decode base64 → raw bytes on each side), then a fixture with a real `sobs-release-dependency-snapshots` artifact + a canned zip of `pip-freeze-linux-amd64.txt`. Until then this is the lone uncovered block of the chain. |
| R35 | **COVERAGE-ONLY, NO Go fix.** `_build_trace_window_overlay_segments` (app.py 14787-14842, ~28 dark lines) — the preserved-window overlay aligned to the trace timeline axis, called from `view_traces` trace-detail at app.py 15594 (`trace_window_segments = _build_trace_window_overlay_segments(all_trace_spans, trace_windows)`). It only runs when BOTH spans and windows are non-empty; `trace_windows` comes from `_list_trace_overlapping_raw_windows` (app.py 2441) = `sobs_raw_windows` rows whose `[WindowStart, WindowEnd]` overlaps the trace AND whose `ServiceName` is `''` or in the trace's services. The prior `tracemetrics`/`tracedetail` goldens seed spans but NO overlapping `sobs_raw_windows` rows, so `trace_windows` was always empty → the builder early-returned `[]` and its whole body (trace-bounds min/max, the `_to_pct` left/width % math, both clamp arms, the `[copied/expected]` title formatting, copy_complete True/False, and the start_pct sort) was dark. | The overlay renders into `templates/traces.html` 523-535 (`raw_window_segments`): per segment, `style="left:{{seg.start_pct}}%; width:{{seg.width_pct}}%;"`, `title="{{seg.title}}"`, and a `complete`/`partial` CSS class from `seg.copy_complete`. These are RELATIVE to the trace timeline (% of `[trace_start, trace_end]`), so they are byte-stable regardless of the now()-anchored absolute epoch and are byte-compared. The Go port — `buildTraceWindowOverlaySegments` (`handlers_pages_traces_detail.go` 280), `traceSpanBounds`, `tsStrToEpochMs`, `listTraceOverlappingRawWindows`/`windowCopyCounts` (`handlers_incident.go`) — was ALREADY complete and byte-faithful (math.Max/Min clamps, `roundHalfEven(…,3)`, identical title concat, stable sort by start_pct): scoped parity GREEN on the FIRST capture and byte-identical across two independent now()-anchored capture cycles. So coverage, not a fix. | NEW `tracewindows` profile (`seed_tracewindows`: clones the tracemetrics now()-anchored single-span `cart-api` trace + 3 gauge rows, ADDS two overlapping `sobs_raw_windows` rows + matching `sobs_raw_window_copy_state`). **Determinism:** otel_metrics' 48h baseline TTL forces the span+gauge rows now()-anchored (mirrors tracemetrics); the windows share the SAME `toStartOfMinute(now())-30min` anchor so they overlap, and because the span lands on a minute boundary (integer epoch-ms) every window edge is a fixed ms offset → the relative %s are exact across all wall clocks. **Two windows exercise the branches:** W1 (`deploy`/`cart@v2`, `ServiceName=cart-api`, `WindowStart=anchor-5min`/`WindowEnd=anchor+5min`) FULLY contains the 80ms trace → both clamp arms fire (`start_ms=max(ws,trace_start)=trace_start`, `end_ms=min(we,trace_end)=trace_end`) → `left:0.0%; width:100.0%`, 3 copy_state rows → `copied_count=3==expected_count=3` → `copy_complete=True` → `complete` class + title `deploy (cart@v2) [3/3]`. W2 (`anomaly`/`latency-p99`, `ServiceName=''` global branch, `WindowStart=anchor+20ms`/`WindowEnd=anchor+60ms`) INTERIOR → NEITHER clamp fires → `left:25.0%; width:50.0%`, 1 copy_state row → `1<3` → `copy_complete=False` → `partial` class + title `anomaly (latency-p99) [1/3]`; segments sort 0.0 before 25.0. Route `get__traces__tracewindows` (profile `tracewindows`, query `trace_id=cccc5555dddd6666eeee7777ffff8888`); masks mirror tracemetrics (mask 1 redacts every now()-anchored DateTime64 string — the trace_start_ts/end pair AND the raw_windows table's per-window window_start/window_end strings; masks 2-3 redact the metric-context JS epoch tokens). The overlay segments are NOT masked → they prove the builder fired. Scoped GREEN 1 / RED 0 / MISSING_GOLDEN 0, byte-stable across two independent now()-anchored runs (both captured 248647B). **Residual arms (classified):** the `we<=0 or ws<=0` skip (unparseable window ts — DEFENSIVE; the DB always returns valid DateTime64 strings); the `end_ms<=start_ms` skip + the `width_pct<=0` skip (a window that overlaps the service/time filter but collapses to zero/negative width after clamping — schedulable via a window whose `[start,end]` touches but does not span any trace interior, e.g. `WindowEnd==trace_start`; partly DEFENSIVE since `_list_trace_overlapping_raw_windows` already filters non-overlapping windows at the SQL level so a true zero-width survivor is an edge case); the empty-spans early return (unreachable here — trace-detail only renders when `detail_rows` is non-empty); the empty-windows early return (already covered by every prior trace-detail golden that seeds no windows). |

**Werkzeug query-encoding reference** (probed from the frozen oracle, WZ 3.1.8): input
`a b!"#$%&'()*+,-./:;<=>?@[\]^_`+"`"+`{|}~` → `a+b!%22%23$%25%26'()*%2B,-./:;%3C%3D%3E?@%5B%5C%5D%5E_%60%7B%7C%7D~`.
Literal: `! $ ' ( ) * , - . / : ; ? @ _ ~` + alnum; space→`+`; escaped: `" # % & + < = > [ \ ] ^ ` + "`` ` `` + `{ | }`.

**Blast radius:** R2/R3/R4/R5 are shared render-layer fixes (every page that builds a `url_for`
querystring with sub-delims, reads `request.args`, does an ordinal string compare, or uses
`|string`). The full Docker parity suite was re-run after the fixes to confirm no regression on the
existing corpus.
